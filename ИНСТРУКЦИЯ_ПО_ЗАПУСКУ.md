# 🚀 Инструкция по запуску проекта

## Шаг 1: Установка Python

Убедитесь, что у вас установлен Python 3.7 или выше.

Проверить можно командой:
```bash
python --version
```

Если Python не установлен, скачайте его с официального сайта: https://www.python.org/downloads/

## Шаг 2: Установка зависимостей

Откройте терминал (PowerShell или Command Prompt) в папке проекта и выполните:

```bash
pip install -r requirements.txt
```

Это установит все необходимые библиотеки:
- Flask
- Flask-SQLAlchemy
- Flask-Login
- Werkzeug
- python-dotenv

## Шаг 3: Запуск приложения

Выполните команду:

```bash
python app.py
```

Вы увидите сообщение:
```
 * Running on http://0.0.0.0:5000
```

## Шаг 4: Открытие в браузере

Откройте браузер и перейдите по адресу:

```
http://localhost:5000
```

или

```
http://127.0.0.1:5000
```

## Шаг 5: Первый вход

При первом запуске автоматически создается администратор:

- **Имя пользователя:** admin
- **Пароль:** admin123

**ВАЖНО:** После первого входа рекомендуется изменить пароль администратора!

## 📝 Что делать дальше?

1. **Зарегистрируйте нового пользователя:**
   - Нажмите "Регистрация"
   - Заполните форму
   - Войдите в систему

2. **Загрузите тестовое видео:**
   - Нажмите "Загрузить видео"
   - Выберите видеофайл (MP4, AVI, MOV и т.д.)
   - Заполните название и описание
   - Нажмите "Загрузить видео"

3. **Протестируйте функционал:**
   - Просмотрите видео
   - Поставьте лайк/дизлайк
   - Оставьте комментарий

4. **Проверьте админ-панель:**
   - Войдите как администратор (admin/admin123)
   - Перейдите в "Админ-панель"
   - Попробуйте ограничить/разрешить видео

## ⚠️ Возможные проблемы

### Проблема: "ModuleNotFoundError: No module named 'flask'"

**Решение:** Установите зависимости:
```bash
pip install -r requirements.txt
```

### Проблема: "Port 5000 is already in use"

**Решение:** Измените порт в файле `app.py`:
```python
app.run(debug=True, host='0.0.0.0', port=5001)  # Измените 5000 на 5001
```

### Проблема: "Permission denied" при загрузке файла

**Решение:** Убедитесь, что папка `uploads` существует и доступна для записи.

### Проблема: Видео не воспроизводится

**Решение:** 
- Убедитесь, что формат видео поддерживается браузером (лучше всего MP4)
- Проверьте, что файл не поврежден
- Попробуйте другой браузер

## 📁 Структура базы данных

База данных создается автоматически в файле `video_hosting.db` при первом запуске.

**Не удаляйте этот файл!** В нем хранятся все данные проекта.

## 🔧 Настройки

Основные настройки находятся в файле `app.py`:

- `SECRET_KEY` - секретный ключ для сессий (измените в продакшене!)
- `MAX_CONTENT_LENGTH` - максимальный размер загружаемого файла (по умолчанию 500MB)
- `UPLOAD_FOLDER` - папка для загруженных видео

## 🛑 Остановка приложения

Для остановки приложения нажмите `Ctrl+C` в терминале.

## 📚 Дополнительная информация

- Подробное объяснение проекта: `ОБЪЯСНЕНИЕ_ПРОЕКТА.md`
- Структура презентации: `ПРЕЗЕНТАЦИЯ.md`
- README: `README.md`

Удачи! 🎉

