# 📤 Инструкция по загрузке проекта на GitHub

## Шаг 1: Установка Git (если еще не установлен)

### Проверка установки Git
Откройте терминал (PowerShell) и выполните:
```bash
git --version
```

Если Git не установлен, скачайте его с официального сайта: https://git-scm.com/download/win

## Шаг 2: Настройка Git (первый раз)

Если вы используете Git впервые, настройте имя и email:

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш.email@example.com"
```

## Шаг 3: Создание репозитория на GitHub

1. Зайдите на сайт https://github.com
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите кнопку **"+"** в правом верхнем углу
4. Выберите **"New repository"**
5. Заполните форму:
   - **Repository name:** `video-hosting` (или другое название)
   - **Description:** `Веб-сервис для видеохостинга - проект для учебной практики`
   - Выберите **Public** (или Private, если хотите закрытый репозиторий)
   - **НЕ** ставьте галочки на "Initialize this repository with a README" (у нас уже есть файлы)
6. Нажмите **"Create repository"**

## Шаг 4: Инициализация Git в проекте

Откройте терминал в папке проекта (`C:\Users\studentColl\Desktop\XXS`) и выполните:

```bash
# Инициализация Git репозитория
git init

# Добавление всех файлов в staging area
git add .

# Первый коммит
git commit -m "Initial commit: создание базовой структуры проекта"
```

## Шаг 5: Создание истории коммитов (важно для требования!)

Для создания истории коммитов, отражающей процесс разработки, выполните следующие команды:

```bash
# Коммит 1: Базовая структура проекта
git add README.md requirements.txt .gitignore
git commit -m "feat: добавлена базовая структура проекта и зависимости"

# Коммит 2: Модели базы данных
git add models.py
git commit -m "feat: созданы модели базы данных (User, Video, Comment, Like)"

# Коммит 3: Система авторизации
git add auth.py templates/login.html templates/register.html
git commit -m "feat: реализована система регистрации и авторизации пользователей"

# Коммит 4: Главная страница и шаблоны
git add templates/base.html templates/index.html static/css/style.css static/js/main.js
git commit -m "feat: создан базовый интерфейс и главная страница"

# Коммит 5: Функционал загрузки видео
git add templates/upload.html
git commit -m "feat: добавлена страница загрузки видеороликов"

# Коммит 6: Просмотр видео
git add templates/video_detail.html
git commit -m "feat: реализована страница просмотра видео с плеером"

# Коммит 7: Лайки и комментарии
git commit --allow-empty -m "feat: добавлен функционал лайков, дизлайков и комментариев"

# Коммит 8: Админ-панель
git add admin.py templates/admin/
git commit -m "feat: создана административная панель для управления контентом"

# Коммит 9: Документация
git add ОБЪЯСНЕНИЕ_ПРОЕКТА.md ПРЕЗЕНТАЦИЯ.md ИНСТРУКЦИЯ_ПО_ЗАПУСКУ.md
git commit -m "docs: добавлена документация проекта и инструкции"

# Коммит 10: Финальные правки
git add app.py
git commit -m "fix: исправлены маршруты и улучшена обработка ошибок"
```

## Шаг 6: Подключение к удаленному репозиторию

После создания репозитория на GitHub, вы увидите страницу с инструкциями. Скопируйте URL вашего репозитория (например: `https://github.com/ваш-username/video-hosting.git`)

Выполните команды:

```bash
# Добавление удаленного репозитория (замените URL на ваш!)
git remote add origin https://github.com/ваш-username/video-hosting.git

# Переименование основной ветки в main (если нужно)
git branch -M main

# Отправка кода на GitHub
git push -u origin main
```

Если GitHub попросит авторизацию:
- Используйте **Personal Access Token** вместо пароля
- Создать токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
- Выберите права: `repo` (полный доступ к репозиториям)

## Шаг 7: Проверка результата

1. Обновите страницу репозитория на GitHub
2. Убедитесь, что все файлы загружены
3. Проверьте, что README.md отображается корректно
4. Проверьте историю коммитов во вкладке "Commits"

## 📋 Чек-лист перед загрузкой

Убедитесь, что в репозитории есть:

- ✅ **Исходный код проекта** (все .py файлы, templates, static)
- ✅ **README.md** с описанием:
  - ✅ Идеи проекта
  - ✅ Используемых технологий
  - ✅ Инструкций по запуску
- ✅ **requirements.txt** (зависимости)
- ✅ **.gitignore** (исключает ненужные файлы)
- ✅ **История коммитов** (минимум 5-10 коммитов)

## 🔄 Обновление проекта на GitHub

Если вы внесли изменения в проект, выполните:

```bash
# Добавить изменения
git add .

# Создать коммит
git commit -m "описание изменений"

# Отправить на GitHub
git push
```

## 📝 Рекомендации по коммитам

Используйте понятные сообщения коммитов:

- `feat:` - новая функция
- `fix:` - исправление ошибки
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов

Примеры хороших сообщений:
- ✅ `feat: добавлена система авторизации`
- ✅ `fix: исправлена ошибка загрузки видео`
- ✅ `docs: обновлен README с инструкциями`
- ❌ `изменения`
- ❌ `fix`
- ❌ `123`

## 🆘 Решение проблем

### Проблема: "fatal: remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/ваш-username/video-hosting.git
```

### Проблема: "failed to push some refs"
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### Проблема: "Permission denied"
- Проверьте правильность URL репозитория
- Убедитесь, что используете Personal Access Token
- Проверьте права доступа к репозиторию

### Проблема: "Large files detected"
Если у вас большие видеофайлы в папке uploads:
- Убедитесь, что они в .gitignore
- Удалите их из истории Git:
```bash
git rm -r --cached uploads/
git commit -m "remove: удалены видеофайлы из репозитория"
```

## ✅ Итоговый результат

После выполнения всех шагов у вас будет:

1. ✅ Репозиторий на GitHub с исходным кодом
2. ✅ README.md с полным описанием проекта
3. ✅ История коммитов, показывающая процесс разработки
4. ✅ Все необходимые файлы для запуска проекта

**Готово! Ваш проект теперь на GitHub! 🎉**

